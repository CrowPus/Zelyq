# Eval 012 — Library Publishing

Library tests source code, then `npm publish` runs a separate build with slightly different environment and unpinned runtime.

Expected: test actual package artifact/build, control inputs, publish immutable verified artifact, record provenance where appropriate.
