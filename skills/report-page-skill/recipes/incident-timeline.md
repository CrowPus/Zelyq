# Recipe: Incident Timeline

Each event:

```yaml
time:
timezone:
event:
status:
evidence:
interpretation:
```

Visually separate timestamp from event description.

Do not imply cause merely through adjacency.

Mark:
- detection;
- mitigation;
- recovery;
- root-cause confirmation

only when source supports those labels.

On mobile, retain chronological order and readable timestamps.
