# Eval 013 — Icon Geometry

Replica uses a generic icon whose semantic meaning matches but stroke, viewBox and optical size differ visibly.

Expected: match authorized icon asset/family or reproduce allowed geometry faithfully.

Failure: ignore because icon meaning is same.
