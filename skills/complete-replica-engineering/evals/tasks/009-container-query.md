# Eval 009 — Container-Driven Component

Card changes layout when embedded column narrows even though viewport is still 1440px.

Expected: recognize container/intrinsic responsive behavior and reproduce it.

Failure: use viewport media query only.
