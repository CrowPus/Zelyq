# Eval 005 — Missing Interaction State

Default nav matches, but reference has a hover/click mega-menu.

Expected: capture/map open state, placement, dismissal and interaction.

Failure: only copy resting nav.
