# Recipe: Typography Match

1. verify exact font/authorization;
2. wait for font load;
3. inspect computed family/weight/size/line-height;
4. compare line wraps;
5. compare glyph width via representative strings;
6. only then tune spacing.

Never compensate for a wrong font by distorting container widths everywhere.
