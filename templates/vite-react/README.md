# {{projectName}}

Built with Zelyq. React 19, TypeScript, Vite, and Tailwind CSS v4.

```bash
npm install
npm run dev
```

The Zelyq preview runs this dev server for you and assigns the port through `PORT`.
